<div class="mt-10 bg-white rounded-xl shadow-md p-6 border border-green-100">
  <h3 class="text-xl font-semibold mb-4 flex items-center gap-2">
    <% if (featuredType === 'bird') { %> 🦜 Bird of the Day <% } else { %> 🌿
    Plant of the Day <% } %>
  </h3>

  <div class="flex flex-col md:flex-row gap-6">
    <div class="md:w-1/3">
      <img
        src="<%= featuredImage %>"
        alt="<%= featuredName %>"
        class="w-full h-48 object-cover rounded-lg shadow-sm"
      />
    </div>

    <div class="md:w-2/3">
      <h4 class="text-lg font-bold text-green-700 mb-1"><%= featuredName %></h4>
      <p class="text-sm text-gray-500 italic mb-3">
        <%= featuredScientificName %>
      </p>

      <p class="text-gray-700 mb-4"><%= featuredDescription %></p>

      <div class="flex flex-wrap gap-2 mb-4">
        <% featuredTags.forEach(function(tag) { %>
        <span class="bg-green-50 text-green-700 text-xs px-2 py-1 rounded-full"
          ><%= tag %></span
        >
        <% }); %>
      </div>

      <a
        href="/species/<%= featuredType %>/<%= featuredId %>"
        class="inline-block text-green-600 hover:text-green-800 font-medium"
      >
        Learn more →
      </a>
    </div>
  </div>
</div>
